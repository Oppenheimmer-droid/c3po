# Backend API URL - se configura en Vercel Environment Variables
NEXT_PUBLIC_API_URL=${RAILWAY_BACKEND_URL}

# Buildtime variables (públicas)
NEXT_PUBLIC_APP_NAME=ReDrive Edu
NEXT_PUBLIC_APP_VERSION=0.1.0
